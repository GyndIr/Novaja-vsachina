<h1>Редактирование задачи</h1>

<%= form_for @task do |f| %>
    <p>
        <%= f.label :title, "Название"%><br>
        <%= f.text_fied :title %>
    </p>
    <p>
        <%= f.label :text, "Описание" %><br>
        <%= f.text_area :text %>
    </p>
    <p>
        <%= f.submit "Сохронить" %>
    </p>

<% end %>

<%= link_to 'Назад', task_path %>